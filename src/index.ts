/**
 * Application entry point.
 *
 * IMPORTANT: dotenv is loaded via --require flag in package.json dev script
 * so environment variables are available before ANY module is evaluated.
 */

// Boot the server
import './server';
